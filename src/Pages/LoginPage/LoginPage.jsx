import DynamicTitle from "../../Components/DynamicTitle/DynamicTitle";

const LoginPage = () => {
    return (
        <div>
            <DynamicTitle
            pageName='Login'
            ></DynamicTitle>
            <h1>Login Page</h1>
        </div>
    );
};

export default LoginPage;